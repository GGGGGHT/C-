#include<iostream>
#include <vector>

using namespace std;

/**
 * 类型转换
 * 在C++中，某些类型之前有关联，如果两种类型有关联，那么当程序需要其中一种类型的运算对象地，可以用另一种关联类型的对象或值来替代。
 * 如果两种类型可以相互转换，那么它们就是关联的
 *
 *
 * @return
 */
int main() {
    // 根据类型转换规则高潜将运算对象的类型统一后再求值，隐式转换
    int ival = 3.51+ 3;

    return 0;
}
